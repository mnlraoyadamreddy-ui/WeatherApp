# Weather App

A simple Android Weather App built with **Kotlin**, **Jetpack Compose**, and **Clean Architecture**.  
It fetches current weather information from **OpenWeatherMap API** and displays it with a user-friendly interface.

---

## 🚀 Features

- Search weather by **US city**.
- **Location-based weather**: asks for location permission and fetches weather automatically.
- **Sunrise and sunset times** converted from Unix timestamps to readable format.
- **Weather icons** displayed with caching.
- **Robust error handling** with user-friendly messages.


---

## 🏗 Architecture

The app uses **Clean Architecture + MVVM**:
